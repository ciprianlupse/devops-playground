name: My First Pipeline

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello DevOps"


      - name: Show Files
        run: ls -la
        
      - name: Show current directory
        run: pwd        
        